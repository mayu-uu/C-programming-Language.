# Approach for Calculating Sum of Digits

We traverse through its digits using a while loop. Take a variable that stores the sum of digits. Until the number becomes 0 we take the modulo 10 of the current number and divide it further by 10.