#include <stdio.h> //stdio header file has information about standard I/O.

int main(){
  printf("Hello , Mayukh here!");
  return 0; //incase of stdlib header is included , exit(0) can also be used.
}
