#include <stdio.h>

typedef long long ll;
typedef long double ld;
typedef double db;
typedef float fl;

int main(){
    int n;
    printf("input number: ");
    scanf("%d", &n);
    int sum=0;
    for(int i=1; i =n; i +){
    sum+=i;
    }
    printf("sum from 1 up to n is: ");
    printf("%d", sum);
    return 0;
}
