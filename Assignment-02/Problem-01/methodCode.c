#include <stdio.h>

typedef long long ll;
typedef long double ld;
typedef double db;
typedef float fl;

int main(){
    ll n;
    printf("input the number: ");
    scanf("%lld", &n);
    int c=0;
    for(ll i=1; i =100; i +){
        if(i%n =0) {printf("%lld\n", i); c +;
        }
    }
    if(c =0){
        printf("no such numbers that are divisible by it exits");
    }
     else{printf("no. of such numbers: ");
    printf("%d", c);}
    return 0;
}